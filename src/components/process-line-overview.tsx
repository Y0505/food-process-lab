"use client";
import { useEffect,useRef,useState } from "react";
import * as THREE from "three";
import { buildSugarcaneVisualizationModel } from "@/processes/sugarcane-visualization-model";
const model=buildSugarcaneVisualizationModel(); const stages=model.stages;
function mat(c:number,metalness=.45,roughness=.42){return new THREE.MeshStandardMaterial({color:c,metalness,roughness});}
function box(g:THREE.Group,s:[number,number,number],p:[number,number,number],m:THREE.Material){const o=new THREE.Mesh(new THREE.BoxGeometry(...s),m);o.position.set(...p);o.castShadow=o.receiveShadow=true;g.add(o);return o;}
function cyl(g:THREE.Group,r:number,h:number,p:[number,number,number],m:THREE.Material,n=24){const o=new THREE.Mesh(new THREE.CylinderGeometry(r,r,h,n),m);o.position.set(...p);o.castShadow=o.receiveShadow=true;g.add(o);return o;}
function pipe(g:THREE.Group,a:THREE.Vector3,b:THREE.Vector3,c=0x4fc09a,r=.06){const d=b.clone().sub(a);const o=new THREE.Mesh(new THREE.CylinderGeometry(r,r,d.length(),12),mat(c,.72,.3));o.position.copy(a).add(b).multiplyScalar(.5);o.quaternion.setFromUnitVectors(new THREE.Vector3(0,1,0),d.normalize());g.add(o);return o;}
function buildLine(animated:THREE.Object3D[]){const g=new THREE.Group(),steel=mat(0x71898d,.82,.28),dark=mat(0x101f24,.72,.34),copper=mat(0x9a704b,.7,.34),cane=mat(0xb18a54,.05,.82),crystal=mat(0xf1dda0,.05,.24),juice=new THREE.MeshStandardMaterial({color:0x48c28d,transparent:true,opacity:.74,emissive:0x123d2d,emissiveIntensity:.4}),vapor=new THREE.MeshStandardMaterial({color:0xc4eeeb,transparent:true,opacity:.28,emissive:0x5baaa5,emissiveIntensity:.9});const xs=stages.map((_,i)=>-8.4+i*2.4);
 box(g,[20.5,.18,4.8],[0,-1.3,0],dark);box(g,[20,.12,.12],[0,3,-.85],steel);box(g,[20,.08,.08],[0,2.78,-.85],steel);for(const x of xs)box(g,[.08,4.25,.08],[x,.85,-.85],dark);for(let i=0;i<9;i++)box(g,[1.7,.025,.035],[-9.5+i*2.4,-1.19,-1.72],mat(0xb8a65c,.05,.65));
 for(let i=0;i<7;i++){pipe(g,new THREE.Vector3(xs[i]+.72,-.05,0),new THREE.Vector3(xs[i+1]-.72,-.05,0));for(let p=0;p<3;p++){const q=new THREE.Mesh(new THREE.SphereGeometry(.055,8,8),juice);q.position.set(xs[i]+.85+p*.38,-.05,0);g.add(q);animated.push(q);}}
 for(let i=0;i<8;i++){const x=xs[i];box(g,[1.7,.12,1.8],[x,-1.19,0],steel);}
 {const x=xs[0];for(let i=-3;i<=3;i++){const r=cyl(g,.18,1.35,[x+i*.2,.3,0],steel,18);r.rotation.z=Math.PI/2;animated.push(r);}box(g,[.9,.8,1.2],[x-.78,1.1,0],dark);for(let i=0;i<7;i++){const c=cyl(g,.065,.65,[x-.7+i*.22,.58,.18],cane,8);c.rotation.z=Math.PI/2;animated.push(c);}}
 {const x=xs[1],d=cyl(g,.76,1.55,[x,.65,0],dark,32);d.rotation.z=Math.PI/2;animated.push(d);const s=cyl(g,.09,1.9,[x,.65,0],steel,14);s.rotation.z=Math.PI/2;animated.push(s);for(let i=0;i<8;i++){const b=box(g,[.14,.09,.52],[x-.55+i*.15,.68,.46],copper);b.rotation.y=i*.22;animated.push(b);}}
 {const x=xs[2];for(const z of[-.38,.38]){const r=cyl(g,.43,1.48,[x,.62,z],steel,32);r.rotation.z=Math.PI/2;animated.push(r);}box(g,[1.75,.16,1.25],[x,-.72,0],dark);cyl(g,.67,.12,[x,-.58,0],juice,28);}
 {const x=xs[3];cyl(g,.7,2.55,[x,.15,0],steel,36);cyl(g,.58,1.15,[x,-.5,0],juice,28);for(const y of[-.25,.12,.49])box(g,[1.1,.055,.12],[x,y,0],dark);}
 {const x=xs[4];for(let i=-1;i<=1;i++){const vx=x+i*.48;cyl(g,.43,1.95,[vx,.22,0],steel,28);for(let j=0;j<4;j++){const r=new THREE.Mesh(new THREE.TorusGeometry(.32,.035,8,24),copper);r.position.set(vx,-.38+j*.28,0);r.rotation.x=Math.PI/2;g.add(r);animated.push(r);}}for(let i=0;i<12;i++){const p=new THREE.Mesh(new THREE.SphereGeometry(.045,8,8),vapor);p.position.set(x-.8+(i%6)*.32,1.05+(i%4)*.3,0);g.add(p);animated.push(p);}}
 {const x=xs[5],t=cyl(g,.74,2.1,[x,.15,0],steel,32);animated.push(t);const l=cyl(g,.62,1.2,[x,-.32,0],juice,28);animated.push(l);const s=cyl(g,.07,2.7,[x,.5,0],dark,12);animated.push(s);for(const y of[-.38,.08,.54])animated.push(box(g,[1.18,.08,.1],[x,y,0],steel));for(let i=0;i<18;i++){const c=new THREE.Mesh(new THREE.OctahedronGeometry(.045),crystal);c.position.set(x-.48+(i%6)*.19,-.65+Math.floor(i/6)*.18,.2*Math.sin(i));g.add(c);animated.push(c);}}
 {const x=xs[6],b=cyl(g,.78,1.35,[x,.2,0],steel,38);b.rotation.z=Math.PI/2;animated.push(b);for(let r=.3;r<.75;r+=.17){const q=new THREE.Mesh(new THREE.TorusGeometry(r,.025,8,32),copper);q.position.set(x,.2,0);q.rotation.x=Math.PI/2;g.add(q);animated.push(q);}}
 {const x=xs[7];box(g,[1.55,2.15,1.5],[x,.12,0],dark);box(g,[1.25,1.55,1.2],[x,.12,0],steel);for(let i=0;i<18;i++){const c=new THREE.Mesh(new THREE.OctahedronGeometry(.04),crystal);c.position.set(x-.48+(i%6)*.19,-.5+(i%5)*.2,-.42+(i%4)*.28);g.add(c);animated.push(c);}}
 return g;}
function pose(i:number){const x=-8.4+i*2.4;return{position:new THREE.Vector3(x*.42,3.55,17.2),target:new THREE.Vector3(x*.38,.1+(i===3?.18:0),0)};}
export default function ProcessLineOverview(){const mount=useRef<HTMLDivElement>(null);const[selected,setSelected]=useState(0);useEffect(()=>{if(!mount.current)return;const el=mount.current,scene=new THREE.Scene();scene.background=new THREE.Color(0x071114);scene.fog=new THREE.Fog(0x071114,13,31);const camera=new THREE.PerspectiveCamera(40,1,.1,60),renderer=new THREE.WebGLRenderer({antialias:true,alpha:true});renderer.setPixelRatio(Math.min(devicePixelRatio,1.6));renderer.shadowMap.enabled=true;renderer.shadowMap.type=THREE.PCFShadowMap;el.appendChild(renderer.domElement);scene.add(new THREE.HemisphereLight(0xc5dfdd,0x101d22,1.55));const light=new THREE.DirectionalLight(0xe2f2ed,3.2);light.position.set(4,8,7);light.castShadow=true;scene.add(light);const floor=new THREE.Mesh(new THREE.PlaneGeometry(28,9),mat(0x0b191e,.15,.9));floor.rotation.x=-Math.PI/2;floor.position.y=-1.42;scene.add(floor);const flow:THREE.Object3D[]=[];const line=buildLine(flow);scene.add(line);const hits=stages.map((_,i)=>{const h=new THREE.Mesh(new THREE.BoxGeometry(1.9,3.7,2.4),new THREE.MeshBasicMaterial({transparent:true,opacity:0,depthWrite:false}));h.position.set(-8.4+i*2.4,.45,0);h.userData.stageIndex=i;line.add(h);return h;});let goal=pose(0),curTarget=goal.target.clone();camera.position.copy(goal.position);camera.lookAt(goal.target);const ray=new THREE.Raycaster(),pt=new THREE.Vector2();const pointer=(e:PointerEvent)=>{const r=renderer.domElement.getBoundingClientRect();pt.set((e.clientX-r.left)/r.width*2-1,-((e.clientY-r.top)/r.height*2-1));ray.setFromCamera(pt,camera);renderer.domElement.style.cursor=ray.intersectObjects(hits,false)[0]?'pointer':'default';};const click=(e:MouseEvent)=>{const r=renderer.domElement.getBoundingClientRect();pt.set((e.clientX-r.left)/r.width*2-1,-((e.clientY-r.top)/r.height*2-1));ray.setFromCamera(pt,camera);const h=ray.intersectObjects(hits,false)[0];if(h){const i=Number(h.object.userData.stageIndex);setSelected(i);goal=pose(i);curTarget=curTarget;window.dispatchEvent(new CustomEvent('food-process-stage-select',{detail:i}));}};renderer.domElement.addEventListener('pointermove',pointer);renderer.domElement.addEventListener('click',click);const onStage=(e:Event)=>{const i=Math.max(0,Math.min(7,Number((e as CustomEvent<number>).detail)));setSelected(i);goal=pose(i);};window.addEventListener('food-process-stage-select',onStage);const timer=new THREE.Timer();let id=0;const loop=()=>{id=requestAnimationFrame(loop);timer.update();const t=timer.getElapsed();line.rotation.y=Math.sin(t*.12)*.035;const b=1-Math.pow(.001,timer.getDelta()*1.35);camera.position.lerp(goal.position,b);curTarget.lerp(goal.target,1-Math.pow(.001,timer.getDelta()*1.65));camera.lookAt(curTarget);flow.forEach((o,i)=>{if(i%7===0)o.rotation.y+=.014;});renderer.render(scene,camera);};loop();const resize=()=>{camera.aspect=el.clientWidth/Math.max(1,el.clientHeight);camera.updateProjectionMatrix();renderer.setSize(el.clientWidth,el.clientHeight,false);};resize();addEventListener('resize',resize);return()=>{cancelAnimationFrame(id);renderer.domElement.removeEventListener('pointermove',pointer);renderer.domElement.removeEventListener('click',click);removeEventListener('food-process-stage-select',onStage);removeEventListener('resize',resize);renderer.dispose();el.removeChild(renderer.domElement);};},[]);const stage=stages[selected];const selectStage=(i:number)=>{setSelected(i);window.dispatchEvent(new CustomEvent('food-process-stage-select',{detail:i}));};return <section className="process-line-overview"><div className="process-line-heading"><div><span>PRODUCTION LINE · FACTORY OVERVIEW</span><h2>Select a machine to understand its role</h2></div><p>The full factory stays visible. Click a machine directly or use the station list, then open details for its internal process.</p></div><div ref={mount} className="process-line-canvas" aria-label="Interactive 3D sugar production line"/><div className="process-line-stages">{stages.map((s,i)=><button key={s.stepId} type="button" className={selected===i?'process-line-stage active':'process-line-stage'} onClick={()=>selectStage(i)}><strong>{String(i+1).padStart(2,'0')}</strong><span>{s.name}</span><small>{s.equipmentId}</small></button>)}</div><div className="process-line-selected"><div><span>SELECTED EQUIPMENT · STAGE {selected+1}</span><strong>{stage.equipmentId}</strong><h3>{stage.name}</h3><p>{stage.description}</p></div><div className="selected-flow"><span>INPUT · {stage.inputStreams.map(s=>s.materialId).join(' + ')}</span><span>OUTPUT · {stage.outputStreams.map(s=>s.materialId).join(' + ')}</span></div><button type="button" className="enter-equipment" onClick={()=>window.dispatchEvent(new CustomEvent('food-process-open-inspection',{detail:selected}))}>View details <b>→</b></button></div></section>;}
